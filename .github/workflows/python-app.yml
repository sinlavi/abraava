name: Test Deploy

on:
  workflow_dispatch:
  push:  # Auto-start on push
  schedule:
    - cron: "0 */3 * * *"  # Run every 3 hours

concurrency:
  group: test-deploy
  cancel-in-progress: true

jobs:
  run-bot:
    permissions:
      contents: write
      actions: write  # Required to trigger a workflow restart via GH CLI
    runs-on: ubuntu-latest
    timeout-minutes: 360

    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 1

      # ========== WARP (Cloudflare proxy) - Fresh IP each time ==========
      - name: Get WARP image digest
        id: warp-digest
        continue-on-error: true
        run: |
          docker pull --quiet caomingjun/warp
          DIGEST=$(docker inspect caomingjun/warp --format='{{.Id}}')
          echo "digest=$DIGEST" >> $GITHUB_OUTPUT

      - name: Cache WARP Docker image
        id: cache-warp
        uses: actions/cache@v4
        continue-on-error: true
        with:
          path: /tmp/warp-image.tar
          key: warp-image-${{ steps.warp-digest.outputs.digest }}

      - name: Load or pull WARP container
        id: warp-pull
        continue-on-error: true
        run: |
          if [[ -f /tmp/warp-image.tar ]]; then
            docker load < /tmp/warp-image.tar
          else
            docker pull caomingjun/warp
            docker save caomingjun/warp -o /tmp/warp-image.tar
          fi

      - name: Remove old WARP data volume for fresh IP
        id: cleanup-warp-data
        continue-on-error: true
        run: |
          docker rm -f warp 2>/dev/null || true
          docker volume rm warp-data 2>/dev/null || true
          echo "Removed old WARP data to get a fresh IP"

      - name: Start Cloudflare WARP proxy (new IP)
        id: warp-start
        continue-on-error: true
        run: |
          docker network inspect warp-net >/dev/null 2>&1 || docker network create warp-net
          docker run -d --name warp --restart=always --network warp-net \
            -p 1080:1080 --cap-add NET_ADMIN \
            --sysctl net.ipv6.conf.all.disable_ipv6=0 \
            --sysctl net.ipv4.conf.all.src_valid_mark=1 \
            -v warp-data:/var/lib/cloudflare-warp \
            caomingjun/warp
          sleep 15  # Increased sleep for proper registration
          
          # Get and display the new IP
          echo "New WARP IP address:"
          curl -x socks5h://127.0.0.1:1080 https://cloudflare.com/cdn-cgi/trace --max-time 10 | grep "ip=" || true
          
          # Verify connection works
          curl -x socks5h://127.0.0.1:1080 https://www.cloudflare.com/cdn-cgi/trace --max-time 10 || true

      # ========== FFmpeg caching ==========
      - name: Cache FFmpeg binaries
        id: cache-ffmpeg
        uses: actions/cache@v4
        continue-on-error: true
        with:
          path: ~/tool_cache/ffmpeg
          key: ffmpeg-release-${{ runner.os }}

      - name: Set up FFmpeg
        id: ffmpeg-setup
        uses: FedericoCarboni/setup-ffmpeg@v3
        continue-on-error: true
        if: steps.cache-ffmpeg.outputs.cache-hit != 'true'
        with:
          ffmpeg-version: release
          architecture: ''
          linking-type: env
          github-token: ${{ github.server_url == 'https://github.com' && github.token || '' }}

      # ========== Auto-Restart Check ==========
      - name: Restart job if WARP or FFmpeg failed
        if: always()
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if [[ "${{ steps.warp-digest.outcome }}" == "failure" ]] || \
             [[ "${{ steps.cache-warp.outcome }}" == "failure" ]] || \
             [[ "${{ steps.warp-pull.outcome }}" == "failure" ]] || \
             [[ "${{ steps.warp-start.outcome }}" == "failure" ]] || \
             [[ "${{ steps.cache-ffmpeg.outcome }}" == "failure" ]] || \
             [[ "${{ steps.ffmpeg-setup.outcome }}" == "failure" ]]; then
          
            echo "WARP or FFmpeg related action failed! Triggering a restart..."
            gh workflow run "${{ github.workflow }}" --ref "${{ github.ref_name }}"
            exit 1
          fi

      # ========== Python & dependencies ==========
      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install Python dependencies
        run: pip install -r requirements.txt

      # ========== Run bot ==========
      - name: Run bot
        id: bot
        continue-on-error: true
        env:
          BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
          OFFLINE_MODE: ${{ secrets.OFFLINE_MODE }}
          DB_CHANNEL_ID: ${{ secrets.DB_CHANNEL_ID }}
          INFO_CHANNEL_ID: ${{ secrets.INFO_CHANNEL_ID }}
        run: python main.py